[project]
name = "dt-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dt-capsolver",
    "httpx>=0.28.1",
    "dt-logger",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dt_scraper"]
artifacts = ["*.json"]

[tool.uv.sources]
dt-logger = { workspace = true }
dt-capsolver = { workspace = true }

[tool.ty.environment]
python-version = "3.13"

[tool.ty.src]
include = ["src/dt_scraper"]
