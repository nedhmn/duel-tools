[project]
name = "dt-logger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "structlog>=25.5.0",
]


[tool.ty.environment]
python-version = "3.13"

[tool.ty.src]
include = ["src/dt_logger"]
