[project]
name = "seeder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aioboto3>=15.5.0",
    "aiometer>=1.0.0",
    "pydantic>=2.12.5",
    "db",
    "parser",
    "logger",
]


[tool.uv.sources]
db = { workspace = true }
parser = { workspace = true }
logger = { workspace = true }

[tool.ty.environment]
python-version = "3.13"

[tool.ty.src]
include = ["src/seeder"]
