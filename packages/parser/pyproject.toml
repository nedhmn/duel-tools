[project]
name = "parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "logger",
    "pandas>=2.3.3",
    "pydantic>=2.12.5",
]


[tool.uv.sources]
logger = { workspace = true }

[tool.ty.environment]
python-version = "3.13"

[tool.ty.src]
include = ["src/parser"]
